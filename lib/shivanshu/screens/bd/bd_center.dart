import 'package:flutter/material.dart';

class BDCenter extends StatelessWidget {
  const BDCenter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
